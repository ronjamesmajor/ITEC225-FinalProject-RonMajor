<Window x:Name="Staffing_Request_Builder" x:Class="ITEC225_FinalProject_RonMajor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ITEC225_FinalProject_RonMajor"
        mc:Ignorable="d"
        Title="MainWindow" Height="800" Width="1280" Background="#41352e">
    <Grid x:Name="formRequestBuilder">
        <TabControl Margin="0,274,0,0" Background="#41352e">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <!--The four values of CornerRadius represent the four corners of the TabItem.-->
                                <Border Name="Border" BorderThickness="1,1,1,0" BorderBrush="#41352e" CornerRadius="8,8,0,0" Margin="-2,0">
                                    <ContentPresenter x:Name="ContentSite"
                                         VerticalAlignment="Center"
                                         HorizontalAlignment="Center"
                                         ContentSource="Header"
                                         Margin="10,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Border" Property="Background" Value="#e3752d" />
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="False">
                                        <Setter TargetName="Border" Property="Background" Value="#a89c99" />
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>
            <TabItem Name="DashBoardTab" Header="Dashboard" Foreground="#41352e">
                <!--<Grid Background="#41352e"/>-->
                <local:DashboardEntry/>
            </TabItem>
            <TabItem Name="RequestsTab" Header="Requests" Foreground="#41352e">
                <Grid Background="#41352e"/>
                <!--<local:TestWindow/>-->
            </TabItem>
        </TabControl>
        <Image x:Name="imgLogo" HorizontalAlignment="Right" Height="100" Margin="10,10,0,0" VerticalAlignment="Top" Width="345" Source="/logo.png"/>
        <Button x:Name="btnNewPosition" Content="New Position" Margin="1150,199,10,0" VerticalAlignment="Top" Height="35" Background="#FFA89C99" Foreground="#FF41352E" BorderBrush="{x:Null}" Click="btnNewPosition_Click">
            <!--Round the corner of the button like we've done everything else.-->
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
        <Button x:Name="btnNewRequest" Content="New Request" Margin="1150,252,10,0" VerticalAlignment="Top" Height="35" Background="#FFE3752D" Foreground="#FF41352E" Click="btnNewRequest_Click" BorderBrush="{x:Null}">
            <!--Round the corner of the button like we've done everything else.-->
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
        <Button x:Name="btnNewCandidate" Content="New Candidate" Margin="1150,144,10,0" VerticalAlignment="Top" Height="35" Background="#FFF8F2F5" Foreground="#FF41352E" BorderBrush="{x:Null}" Click="btnNewCandidate_Click">
            <!--Round the corner of the button like we've done everything else.-->
            <Button.Resources>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Button.Resources>
        </Button>
        
    </Grid>
</Window>
